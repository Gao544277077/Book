extends layout
  
block content
        br
        br
        br
        h1 #{title}
        ul.list-group
            each book, i in books
                li.list-group-item
                    a(href="/books/book/"+book.id)=book.id